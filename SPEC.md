# Cairn store specification

Stub. This document will define the `.cairn/` store format: directory layout,
the entry and trace JSON Schemas, the entry lifecycle, and spec versioning
rules. See `documentation/readme.md` for the design this spec will formalize.
